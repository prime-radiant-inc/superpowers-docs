# <Project> — what it is, who it's for

<!--
Instantiation notes (delete after instantiating):
- Every capability bullet carries its cash: the doc/section or code surface
  that enacts it. No citation, no claim.
- Costs land with whoever bears them. Never sell one reader's burden as
  another reader's benefit.
- Birth goes through the new-docs flow (index row, classification confirm,
  verification, stamp) — this template is the section shape, not a bypass.
- No stamp sentinel here: the first `docmaint stamp --set` appends the block
  itself; a bare sentinel makes stamping raise.
-->

<one sentence a stranger parses: what it is and what it does for whom>

## What you get

<the user's transformation, user-as-subject, problem beat first; zero
architecture>

## Using it

<the experience: the first-success path on an existing deployment, real
commands with really-produced output>

## Running it

<owner/operator: how it works, what you deploy and operate, isolation and
control surfaces, what it costs to run>

## Who it's for — and not for

<fit by role; "not for" stated as positioning, not apology>

## Limitations

<each tagged by who bears it: (user) / (operator)>

## Getting started

<two paths: the user's (enroll/use) and the owner's (deploy guide pointer)>

<!-- brochure.html is rendered from this file by the superpowers-docs
documentation skill; re-render on stamp-SHA mismatch (see the sentinel in
brochure.html). -->
