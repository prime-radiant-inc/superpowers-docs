Done. The brochure exists in both forms, governed end to end, and everything is left uncommitted on `docs/brochure` for your review:

**What's in the tree** (`git status`: 2 modified, 2 new):
- **`docs/BROCHURE.md`** — the governed positioning doc, in the template's section shape. It opens with the README's existing one-liner ("One clipboard across a fleet of macOS + Linux hosts.") so the README already carries it verbatim, as the flow requires. Every capability bullet cites the doc section or code surface that enacts it; limitations are tagged `(user)`/`(operator)`; the license is stated plainly (proprietary, public repo, signed releases). The `clipfan --help` block is real output from a binary I built from this tree. Stamped: `2026-06-10 · 9b9970e · 11 claims deferred`.
- **`docs/brochure.html`** — an 18 KB self-contained dark-keynote deck, 11 slides in three acts plus a close: what-you-get (user), using-it (user), running-it (owner, warm-accent shift), then one door per cast member. Every slide has a citation footer ending `verified @ 9b9970e`; the first line is the generated-file sentinel pinning the same SHA, so `docmaint scan` skips it and the regeneration cursor is armed.
- **`docs/INDEX.md`** — new BROCHURE.md row (Reader `adopter`, evergreen, Owns = the enacting surfaces it cites), brochure.html noted in the excluded comment, and the decided-gaps record updated as you directed: the other three gaps are preserved verbatim, and the brochure clause now records that you reopened it on 2026-06-10 because the "internal proprietary tool" rationale had gone stale — I confirmed the repo is genuinely public (unauthenticated GitHub API returns 200).
- **`README.md`** — a pointer to the brochure at the top of the Documentation list; nothing restated.

**Verification:** two adversarial subagents (one attacking claims, one auditing voice/story/design) found **zero false or fabricated claims** — one rebuilt the binary and byte-checked the help output, and traced things like the concealed-clip drop path and nonce-replay rejection to the enacting code. They did find real defects — footers that didn't cite what cashed their cards, two deck details the source doc didn't carry, one X-not-Y over budget, an unmarked elision — all fixed, and a third independent agent confirmed all 12 fixes landed before I stamped. The 11 deferred claims are external-truth items (OSC 52 terminal behavior, Codex/Claude Code paste behavior, Sequoia's gate, notarization, and "the repo is public" — that last one is carried by no stamped doc, so it rides deferred until a doc owns it).

**Decisions I made that you may want to veto at review:** voice resolved to the default engineer preset (the dictionary has no `## Voice` section — say the word if you want one bootstrapped); the cast is user + owner as the same person in two moments; the thesis is "copy on any machine and it's already everywhere you'll paste — including an image into a terminal agent on a headless box." One known caveat: the stamp and sentinel pin `9b9970e`, the audited tree the claims were verified against — consistent with today's audit convention, but since the files are new and uncommitted, the first restamp after you commit will pin a commit that actually contains them.